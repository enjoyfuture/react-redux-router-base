<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <meta charset="UTF-8">
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="0">
    <!--360浏览器-->
    <meta name="renderer" content="webkit">
    <!--其它双核浏览器-->
    <meta name="force-rendering" content="webkit">
    <!--如果安装了GCF，则使用GCF来渲染页面，如果没有安装GCF，则使用最高版本的IE内核进行渲染。-->
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="keywords" content="">
    <meta name="description" content="">
    <!-- viewport -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <!--删除 IOS 默认的工具栏和菜单栏-->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <!--作用是控制状态栏显示样式，可选default、black、black-translucent-->
    <meta name="apple-mobile-web-app-status-bar-style" content="black">
    <!--禁止ios上自动识别电话-->
    <meta name="format-detection" content="telephone=no">
    <!--禁止android上自动识别邮箱-->
    <meta name="format-detection" content="email=no">
    <link rel="icon" href="{{{urlContext}}}/favicon.ico" type="image/x-icon">

    <!-- dsn 预加载 -->

    <!--系统样式文件-->
    {{{links}}}
  </head>
  <body>
    <div id="layout"></div>

    {{{body}}}
    <!--服务端渲染的首屏初始值 -->
    <script>{{{scriptHtml}}}</script>

    <!--嵌入式控制台 -->
    <script>
      (function () {
        var _make_rnd = function () {
          return (+new Date()) + '.r' + Math.floor(Math.random() * 1000);
        };
        // 全局异常监听，方便调试
        window.onerror = function (errorMessage, scriptURI, lineNumber, columnNumber, errorObj) {
          var errorMsg = '错误信息：' + errorMessage + ',出错文件：' + scriptURI + ',出错行号：' + lineNumber
              + ',出错列号：' + columnNumber + ',错误详情：' + JSON.stringify(errorObj);
          var win = window;
          var n = 'jsFeImage_' + _make_rnd(),
              img = win[n] = new Image();
          img.onload = img.onerror = img.onabort = function () {
            win[n] = null;
            img.onload = img.onerror = img.onabort = null;
          };
          img.src = '/client-error?i=' + encodeURIComponent(errorMsg);
        };

      })();
    </script>

    <!--页面用到的 js -->
    {{{scripts}}}
  </body>
</html>
