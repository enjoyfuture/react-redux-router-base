import classNames from 'classnames/bind';
import styles from 'bootstrap/dist/css/bootstrap.css';

const bootstrap = classNames.bind(styles);

export const bootstrapStyles = styles;

export default bootstrap;